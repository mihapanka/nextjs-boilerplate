import { RegistrationFlow } from "@/components/registration-flow";

export default function BClassRegistrationPage() {
  return (
    <RegistrationFlow
      classKey="b"
      classLabel="B osztály"
      intro="Ez a B osztály számára kialakított jelentkezési útvonal. A lépések ugyanazt a letisztult, könnyen átnézhető folyamatot követik minden család számára."
    />
  );
}
